{
  "name": "trippingly",
  "private": true,
  "workspaces": [
    "frontend",
    "backend/functions"
  ],
  "scripts": {
    "start:dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run serve --workspace=functions\"",
    "start:prod": "npm run build --workspace=frontend && npm run deploy --workspace=functions"
  },
  "dependencies": {
    "firebase-functions-test": "^3.4.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
